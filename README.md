# ClawSetupUtility

A single-file setup utility that downloads ClawAssembler, claw-vm and ClawAudioStudio.
It requires Lua and its socket, ltn12 (socket too probably), zip, luacurl and lfs libraries (. These are probably standard).
